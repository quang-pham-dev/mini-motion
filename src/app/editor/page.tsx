import type { Metadata } from 'next';
import { Editor } from '@/features/editor';

export const metadata: Metadata = {
  title: 'Script Editor',
  description: 'Create and edit your video scripts with AI',
};

export default Editor;
