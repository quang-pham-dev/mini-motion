import type { Metadata } from 'next';
import { Dashboard } from '@/features/dashboard';

export const metadata: Metadata = {
  title: 'Dashboard',
  description: 'Manage your AI video projects',
};

export default Dashboard;
